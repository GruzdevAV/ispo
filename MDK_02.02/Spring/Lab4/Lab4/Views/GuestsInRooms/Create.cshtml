@model Lab4.Models.GuestsInRoom

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>


@using (Html.BeginForm()) 
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>GuestsInRoom</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div class="form-group">
            @{ var ROOM_ID_NAME = Html.DisplayNameFor(model => model.ROOM_ID);}
            @Html.LabelFor(model => model.ROOM_ID, ROOM_ID_NAME.ToHtmlString(), htmlAttributes: new { @class = "control-label col-md-2" })
            @*@Html.LabelFor(model => model.ROOM_ID, "ROOM_ID", htmlAttributes: new { @class = "control-label col-md-2" })*@
            <div class="col-md-10">
                @Html.DropDownList("ROOM_ID", null, htmlAttributes: new { @class = "form-control" })
                @Html.ValidationMessageFor(model => model.ROOM_ID, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            @{ var GUEST_ID_NAME = Html.DisplayNameFor(model => model.GUEST_ID);}
            @Html.LabelFor(model => model.GUEST_ID, GUEST_ID_NAME.ToHtmlString(), htmlAttributes: new { @class = "control-label col-md-2" })
            @*@Html.LabelFor(model => model.GUEST_ID, "GUEST_ID", htmlAttributes: new { @class = "control-label col-md-2" })*@
            <div class="col-md-10">
                @Html.DropDownList("GUEST_ID", null, htmlAttributes: new { @class = "form-control" })
                @Html.ValidationMessageFor(model => model.GUEST_ID, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
